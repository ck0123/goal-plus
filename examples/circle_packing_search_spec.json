{
  "objective": "maximize valid 26-circle packing radius sum in the unit square",
  "metric_name": "combined_score",
  "metric_direction": "maximize",
  "source_path": "tests/fixtures/circle_packing",
  "edit_surface": {
    "allow": [
      "initial_program.py"
    ],
    "deny": [
      "evaluator.py",
      "config.yaml"
    ]
  },
  "process_verifiers": [
    {
      "name": "circle_packing_score",
      "role": "ranking_signal",
      "command": [
        "python",
        "-c",
        "from evaluator import evaluate; import json; print(json.dumps(evaluate('initial_program.py'), sort_keys=True))"
      ],
      "timeout_seconds": 30
    }
  ],
  "promotion_verifiers": [
    {
      "name": "evaluator_hash_check",
      "role": "anti_cheat_gate",
      "command": [
        "search-runtime-internal",
        "check-frozen-hashes"
      ]
    }
  ],
  "budget": {
    "max_candidates": 8,
    "max_parallel": 4,
    "wall_clock_seconds": 600
  },
  "strategy": "independent_branches",
  "constraints": {
    "suggested_batch_size": 4,
    "task_summary": "Place exactly 26 non-overlapping circles inside a unit square and maximize the sum of radii."
  }
}
