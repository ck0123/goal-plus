/goal-plus mode=autonomous Optimize only solution.py to minimize score_cycles while preserving complete correctness and generality. Use python3 runner.py and the public verifier as the evaluation basis; do not inspect files outside this repository, use network access, hard-code public or hidden seeds, or modify problem.py, runner.py, verifier.py, tests, or test cases.

This is a controlled single-lane Codex plus Goal Plus ablation. Freeze one Search run with budget.max_candidates=1 and budget.max_parallel=1. Start exactly one Codex candidate workspace and give its initial worker dispatch worker_budget.max_runtime_seconds=7200. The candidate worker owns the technical research strategy end to end. The main agent must not propose implementation directions, summarize alternative technical ideas for it, or create additional candidates.

Measure a two-hour worker experiment beginning when the candidate worker is first launched. If that worker returns before the two-hour experiment window has elapsed and the objective remains open, call search_continue_agent_session for the same agent_session_id with the remaining runtime budget and invoke the returned followup_task. Use only this neutral continuation directive: "Continue autonomously optimizing solution.py." Do not call search_plan_next to replace it or create another candidate.

If native same-worker continuation is no longer available, call search_redispatch_candidate for the same candidate workspace with the remaining runtime budget and the same neutral directive. This is recovery of one lane, not a new research direction. Repeat continuation or same-candidate redispatch as necessary until the two-hour worker window is exhausted, a confirmed infrastructure fault requires stopping, or the worker provides concrete verifier-backed evidence that no materially promising direction remains.

The worker should call search_run_verifier after meaningful solution.py changes and retain the best correct verifier-backed artifact. The main agent should only enforce the frozen evaluation contract, worker lifecycle and remaining budget; it must not manage the candidate's technical content. After the worker window closes, run the main final verification, select the sole candidate's best eligible iteration, report, promote, and complete the Goal Plus audit.
