/goal-plus mode=autonomous Run one preregistered VLIW SpaceAgent experiment, experiment __EXPERIMENT_ID__. Use exactly three concurrent candidate lanes for one hour, with SpaceAgent enforcement enabled and no control arm.

The Goal Plus main agent is an orchestration shell only. It may freeze the public evaluation contract, create and link one Search run, open its global SpaceAgent, launch and monitor the candidates, enforce the worker leases, perform final verification, select and promote the best verifier-backed result, and finish the audit. It must not inspect or edit solution.py, analyze optimization choices, suggest directions, or pass technical information between candidates.

Freeze this Search contract without asking the user:

- source_path is exactly `__WORKSPACE__`; objective is to minimize the correct VLIW simulated cycle count by editing solution.py; metric_name is cycles and metric_direction is minimize.
- Allow edits only to solution.py, with max_file_changes=1. Protect problem.py, runner.py, verifier.py, test_cases/, .goal-plus-verifiers/, .codex/, experiment.json, run_experiment.py, and space-schema.json.
- Use `python3 .goal-plus-verifiers/vliw_score.py` as the ranking_signal process verifier with cwd `.`. Freeze that script plus problem.py, runner.py, verifier.py, and test_cases/public_cases.json as verifier artifacts.
- Set budget.max_candidates=3, budget.max_parallel=3, and workspace.backend=git_worktree.
- Set strategy.name=random, strategy.orchestration_mode=parallel_loops, and strategy.worker_host=codex.
- Set strategy.worker_budget to `{ "min_runtime_seconds": __MIN_RUNTIME_SECONDS__, "min_verifier_runs": 1, "max_runtime_seconds": __MAX_RUNTIME_SECONDS__, "on_exceed": "interrupt" }` and strategy.worker_launch to `{ "model": "__MODEL__", "reasoning_effort": "__REASONING_EFFORT__" }`.

After search_create and goal_plus_link_search_run, call search_space_open once with mode="enforce", schema_path="space-schema.json", experiment_id="__EXPERIMENT_ID__", reviewer_model="__MODEL__", reviewer_reasoning_effort="__REASONING_EFFORT__", reviewer_timeout_seconds=180, and schema_consolidation_interval=20.

Call search_plan_next(requested_k=3) and search_start_batch once. Materialize c001, c002, and c003, call search_start_agent_session exactly once for each candidate and never create another session, then launch all three returned Codex payloads before waiting. Bind each native handle to its own session; use search_continue_agent_session for continuation. Follow the returned worker-runtime and watchdog protocol independently for every lane; do not replace candidates, add technical follow-ups, or stop the experiment because a lane regresses or finishes early.

At the experiment boundary, drain all lanes and complete the required parent verification for each candidate without candidate intervention metadata. Select and promote the best eligible verifier-backed result, record it in Goal Plus, finish the audit, and generate the final Search report. The selected candidate is not predetermined.

Give every candidate the following task verbatim.

--- BEGIN EXACT CANDIDATE TASK ---

Optimize solution.py as much as possible to minimize the correct cycle count returned by the Goal Plus verifier.

Work autonomously and continuously for the assigned lease. Do not use Goal Plus orchestration, subagents, network access, files outside this repository, hidden evaluator data, sibling workspaces, prior experiment output, or transcripts. Edit only solution.py. Do not run runner.py or any scorer directly; use search_run_verifier for every measurement.

Start with search_get_agent_context using the supplied agent_session_id. Read problem.py, runner.py, verifier.py, solution.py, the public test-case shape, context.results_tsv, and existing verifier-backed iterations. Then repeatedly identify the current bottleneck, formulate one concrete falsifiable experiment, obtain SpaceAgent admission, implement and measure the accepted experiment, and continue from the strongest evidence-backed implementation.

Before every material edit or evaluator run, call search_space_propose in a separate tool call with exactly these PlanCard fields:

- intervention: the concrete material change to execute;
- scope: the affected code surface, parameters, workload context, and boundaries;
- expected_new_information: the uncertainty the experiment will resolve.

If rejected, do not execute or partially execute that plan. Use duplicate_of and duplicate_plans only to understand what it duplicates, then independently formulate a materially distinct plan; SpaceAgent provides no direction. If accepted, execute only that plan and close it with exactly one search_run_verifier call carrying context.agent_session_id, the accepted plan_id as intervention_plan_id, and a concise hypothesis. Wait for that result before proposing another plan.

Treat every valid result, including a neutral or regressed one, as evidence. Compare it with your best correct result and decide whether to keep, revert, or build further. Do not stop after failures or at an apparent local optimum; reassess the critical path, instruction-slot pressure, dependencies, scratch/register constraints, and applicable lower bounds, then choose another falsifiable experiment.

Keep the strongest correct verifier-backed implementation. Before returning, restore that exact verifier-backed Git revision and write the required bounded .tmp/handoff.json directly; exact incumbent restoration and handoff metadata are lifecycle operations, not new experiments. Any new material solution change still requires an accepted plan and verifier. Return a concise evidence summary.

--- END EXACT CANDIDATE TASK ---
